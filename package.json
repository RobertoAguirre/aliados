{
  "name": "aliadosqr",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && rm -rf node_modules && npm install && npm run build",
    "start": "cd backend && npm install && node src/index.js"
  }
}

