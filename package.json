{
  "name": "aliadosqr",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && npm install && cd ../frontend && npm install",
    "build": "npm install && cd frontend && rm -rf node_modules && npm install && npm run build",
    "start": "cd backend && node src/index.js"
  }
}

